# خطة تنفيذ النظام المركزي (Centralized SASMAN)

- [x] ### 1. استمرارية الجلسات
    - تحديث `SQLite` لتخزين التوكنات والمنافذ.
- [ ] ### 2. نظام النسخ الاحتياطي التلقائي (Agent to Central)
    - إضافة Endpoint في السيرفر لاستقبال قاعدة بيانات الوكيل.
    - تشغيل `cron job` في الوكيل لرفع `data/sasman.db` كل يوم الساعة 12 مساءً.
    - الاحتفاظ فقط بآخر نسخة (`latest_backup.db`) لكل وكيل في السيرفر المركزي.
- [ ] ### 3. بوابة التنزيل (Download Interface)
    - إضافة صفحة/رابط للمسؤول (أو العميل) لتحميل آخر نسخة احتياطية خاصة به.
- [ ] ### 4. الحماية والأمن
    - تطبيق التحقق من التوكنات للرفع والتحميل.
- [ ] ### 5. الاختبار والبناء
    - إضافة `test_build.sh` لاختبار بناء السيرفر والوكيل معاً وتأكيد سلامة التكامل.
