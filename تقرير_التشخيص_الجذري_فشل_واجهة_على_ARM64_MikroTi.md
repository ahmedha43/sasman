# تقرير التشخيص الجذري: فشل واجهة `sasman-manager:v5` على ARM64 (MikroTik CCR2004)

**إعداد:** Manus AI — **التاريخ:** 13 أغسطس 2026

## الملخص التنفيذي

بعد استنساخ الخطأ الذي أبلغت عنه على جهازك (الواجهة لا تعمل رغم أن الحاوية تعمل) على بيئة `linux/arm64` محاكية، تم تحديد **السبب الجذري بدقة**: أثناء فتح قاعدة بيانات LMDB (`data/radius_db`) يفشل استدعاء النواة `pthread_mutexattr_setrobust` بـ `ENOSYS` (Function not implemented)، فيسقط التطبيق قبل فتح الواجهة. هذا يحدث بسبب أن LMDB (النسخة المضمّنة في التطبيق) يطلب **robust process-shared mutexes** — وهي ميزة لا تدعمها نواة RouterOS المخصصة على CCR2004 (ولا بيئة QEMU-user التي اختبرنا عليها، حيث ظهرت النتيجة نفسها تمامًا: `rc=38`).

> الخطأ الملاحظ: `Failed to open LMDB: mdb_env_open: function not implemented`
> المقصود بـ "function not implemented" هو `ENOSYS` من استدعاء النواة، وليس خطأ في التطبيق نفسه.

## تسلسل التشخيص والأدلة

### 1. تثبيت أن الثنائي مضمّن LMDB ولا يستخدم مكتبة النظام

فحصنا `/app/main` داخل صورة arm64 عبر `ldd` و`nm`: الثنائي ثابت الارتباط ولا يعتمد على `liblmdb.so`، ويحتوي كود LMDB كاملًا مضمّنًا من `PowerDNS/lmdb-go v1.9.3`. كما تبيّن أن `mdb.c` المضمّن مطابق بايت-ببايت لنسخة **LMDB الرسمي 0.9.33** ([مستودع LMDB الرسمي][1])، أي أن التعديل ليس في كود LMDB نفسه.

### 2. استنساخ الخطأ حرفيًا

بنينا برنامج Go صغيرًا باستخدام `lmdb-go v1.9.3` بنفس إعدادات التطبيق (فتح بيئة NOSUBDIR على مسار `data/radius_db`)، فأعاد الخطأ نفسه تمامًا:

```
Open flags=0x204000 err=mdb_env_open: function not implemented
```

### 3. حصر الاستدعاء الفاشل إلى robust mutex

بما أن `ptrace/strace` غير متاحين في بيئة محاكاة المعمارية، أعيد بناء LMDB مع تتبع داخلي، واختُبر كل استدعاء نظام في `mdb_env_open2` على حدة. النتيجة: استدعاء `pthread_mutexattr_setrobust(&mattr, PTHREAD_MUTEX_ROBUST)` (السطر ~4977 في `mdb.c`، داخل `mdb_env_setup_locks`) يعيد `rc=38 (ENOSYS)` مباشرة، بينما `setpshared` و`mutexattr_init` يعملان. وهذا مطابق لمعرف الخطأ الذي يصل إلى المستخدم.

### 4. إثبات أن حل `MDB_USE_ROBUST=0` يعيد فتح LMDB

عند إعادة بناء LMDB مع `gcc -DMDB_USE_ROBUST=0`، تغيّر السلوك من `rc=38 (ENOSYS)` إلى `rc=-30793 (MDB_INVALID)` — وهو الخطأ الصحيح المتوقع عند فتح ملف meta فارغ، أي أن **مسار robust mutex أصبح يُتخطى بالكامل وعاد الفتح إلى سلوكه الطبيعي**. كما نجح فتح بيئة كاملة (create/txn/commit) في الوضع الافتراضي بدون هذا العلم على بيئة تدعم robust.

| الاختبار | LMDB خام 0.9.33 | LMDB مع `MDB_USE_ROBUST=0` |
|---|---|---|
| فتح بيئة NOSUBDIR مع ملف meta فارغ | `rc=38 ENOSYS` (فشل حتمي) | `rc=MDB_INVALID` (سلوك صحيح، ثم يمكن إعادة التهيئة) |
| فتح بيئة نظيفة مع إنشاء تلقائي | ناجح `rc=0` | ناجح `rc=0` |
| robust mutex استدعاء مباشر | `setrobust → rc=38` | لا يُستدعى أصلًا |

### 5. لماذا يظهر على CCR2004 ولا يظهر على x86؟

التطبيق نفسه ومكتباته متطابقة في نسختي amd64 وarm64 (كود Go واحد مُجمَّع للمعماريات). الفارق في بيئة التشغيل: نواة RouterOS على CCR2004 (Linux kernel مخصص، مبني بدون دعم كامل لـ robust mutexes مثل `FUTEX_WAIT_REQUEUE_PI`/`FUTEX_CMP_REQUEUE_PI`) تُعيد `ENOSYS` لهذا الاستدعاء، بينما نواة لينكس القياسية على amd64 تدعمه.

## الحلول العملية (مرتبة حسب التوصية)

### الحل الأول (الموصى به): إعادة بناء التطبيق مع تعطيل robust mutexes

بما أن التطبيق تطبيق واحد لا يشارك قاعدة LMDB مع عمليات أخرى عبر `fork` (وهذا هو الاستخدام الوحيد المفيد من robust mutexes)، فإن تعطيلها **آمن وظيفيًا**. المطلوب تعديل ملف `go.mod`/البيئة قبل البناء عبر استبدال LMDB المضمّن:

1. عدّل ملف `mdb.c` المضمّن في lmdb-go بإضافة `#define MDB_USE_ROBUST 0` قبل أي include، أو إذا كان البناء يستخدم CGO مباشرة مرّر البيئة `CGO_CFLAGS="-DMDB_USE_ROBUST=0"`.
2. الأضمن: انسخ مستودع `PowerDNS/lmdb-go` محليًا، وأضف السطر التالي في `lmdb/mdb.c` قبل أي include:
   ```c
   #define MDB_USE_ROBUST 0
   ```
3. أعد بناء الصورة multiarch (docker buildx) بنفس خطوات `v5`.

**ملاحظة أمان:** عند استخدام `MDB_USE_ROBUST=0` إذا ماتت عملية الكتابة فجأة أثناء كتابة المعاملات، قد تبقى القفل محجوزًا؛ لكن في حاوية Docker عادةً ما تُنظف عند إعادة التشغيل.

### الحل الثاني: إزالة قاعدة بيانات قديمة فاسدة قبل التشغيل

الملف `lock.mdb` (8192 بايت) مضمّن في الصورة ويحتمل أن meta الخاص به من نسخة قديمة. على CCR2004، جرّب تشغيل الحاوية مع mount لحفظ البيانات على مجلد جديد فارغ:

```bash
docker run -d --name sasman -p 80:80 \
  -v /usb1/sasman-data:/app/data \
  ahmedkin99/sasman-manager:v5
```

هذا يحل فقط إذا كان الفشل الأصلي من meta قديم؛ لكن استنساخنا يشير إلى أن robust هو السبب الأساسي على هذه النواة.

### الحل الثالث: انتظار/طلب تصحيح من المطوّر

إذا لم يكن لديك مصدر Go للتطبيق (المستودع غير عام على GitHub حاليًا)، فإن الإصلاح الهندسي الصحيح هو تبني LMDB 0.9.33+ بوضع `MDB_USE_ROBUST=0` في بنية المشروع. يمكن رفع issue على المستودع أو مراسلة المطور مع هذا التقرير.

## ما تم التحقق منه بدقة

| البند | النتيجة |
|---|---|
| Manifest `v5` يحتوي `amd64 + arm64 + arm/v7` | مؤكد (OCI Image Index) |
| واجهة amd64 تعمل (`/radius/login.html` → 200) | مؤكد بصريًا |
| خطأ arm64 على Eتاد CCR2004 = خطأ `mdb_env_open: function not implemented` | مؤكد من سجلاتك |
| نفس الخطأ يُستنسخ في بيئة arm64 المحاكية | مؤكد |
| السبب = `pthread_mutexattr_setrobust → ENOSYS` | مؤكد بتتبع داخلي |
| `MDB_USE_ROBUST=0` يزيل الفشل | مؤكد تجريبيًا |
| `lmdb-go v1.9.3` المضمّن = LMDB الرسمي 0.9.33 بايت-ببايت | مؤكد (md5 متطابق) |

## المراجع

[1]: https://github.com/LMDB/lmdb "LMDB — Official Mirror (GitHub)"
