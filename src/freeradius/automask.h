/*
 * Alpine's freeradius-dev package references this generated header from
 * autoconf.h but does not ship it.  The module does not use the generated
 * mask helpers, so an empty compatibility header is enough for compilation.
 */
