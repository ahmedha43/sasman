# هل يمكن استخدام LMDB لكافة بيانات النظام؟

هذا المستند يجيب على سؤال معماري مهم: **هل يمكننا التخلي عن SQLite واستخدام LMDB لتخزين كافة بيانات نظام SASMAN بالكامل؟**

## الإجابة القصيرة
**نعم، من الناحية التقنية البحتة "يمكن" ذلك، ولكن من الناحية العملية والهندسية "لا يُنصح بذلك".** الأفضل هو الاعتماد على **بنية هجينة (Hybrid Architecture)** تجمع بين ميزات الاثنين.

---

## مقارنة بين LMDB و SQLite

لنفهم السبب، يجب أن نعرف طبيعة كل نظام:

### 1. LMDB (Lightning Memory-Mapped Database)
هو عبارة عن مخزن "مفتاح-قيمة" (Key-Value Store). لا يفهم الجداول ولا العلاقات ولا لغة SQL. يعتمد على حفظ البيانات مباشرة في الذاكرة ومزامنتها بذكاء مع القرص الصلب.
*   **المميزات:**
    *   **سرعة خرافية:** من أسرع قواعد البيانات في العالم لعمليات القراءة، ممتاز للأنظمة التي تتعرض لضغط هائل.
    *   **صديق للذواكر المؤقتة (Flash Storage):** بما أنه Memory-Mapped، فإنه يقلل من عمليات الكتابة على القرص، مما يجعله مثالياً لأجهزة مثل راوترات المايكروتك.
    *   **مستهلك قليل للموارد:** لا يحتاج إلى عمليات معالجة (CPU) عالية لجلب البيانات.
*   **العيوب:**
    *   **غياب الاستعلامات المعقدة:** لا يمكنك البحث عن "المشتركين الذين لم يدفعوا هذا الشهر وتنتهي باقتهم غداً".
    *   **غياب العلاقات (Relations):** لا يمكنك ربط جدول "المستخدمين" بجدول "المدراء" بسهولة.

### 2. SQLite
هو قاعدة بيانات علائقية (Relational Database) مصغرة وممتازة، تدعم الجداول والروابط ولغة SQL.
*   **المميزات:**
    *   **مرونة الاستعلام (SQL):** يمكنك كتابة أي استعلام مهما كان معقداً للبحث والفلترة واستخراج التقارير.
    *   **ترتيب وهيكلة البيانات:** وجود الجداول والروابط (Foreign Keys) يمنع تضارب البيانات ويجعل النظام منظماً.
*   **العيوب:**
    *   أبطأ نسبياً من LMDB تحت الضغط العالي جداً.
    *   يستهلك عمليات كتابة أكثر (Disk I/O) مقارنة بـ LMDB، مما قد يؤثر على ذواكر أجهزة الراوتر الصغيرة على المدى الطويل جداً (إذا لم يتم تحسينه).

---

## ماذا لو استخدمنا LMDB لكل شيء؟

إذا قررنا وضع كل شيء (المستخدمين، الباقات، أجهزة الراديوس، المدراء، الإعدادات، الفواتير، الديون) في LMDB، فسنواجه **تحديات برمجية ضخمة**:
1.  **برمجة محرك استعلامات خاص:** سنضطر لكتابة كود Go معقد جداً لفرز وتصفية البيانات بدلاً من استخدام أمر SQL بسيط مثل `SELECT`.
2.  **صعوبة بناء لوحة التحكم:** الواجهات الإدارية (Admin Panels) تعتمد بشكل كبير على البحث والترتيب والتصفية، وهي أمور صعبة ومكلفة من ناحية الـ (RAM) عند إجرائها على Key-Value store للبيانات الكبيرة.
3.  **صعوبة التقارير:** إنشاء تقرير مالي أو كشف حساب سيكون عملية بطيئة برمجياً.

---

## النهج المتبع في SASMAN Enterprise (النظام الهجين)

للحصول على أفضل ما في العالمين (أداء استثنائي + إدارة مرنة)، يستخدم نظام SASMAN بنية هجينة تدمج قوة LMDB و SQLite:

### 🎯 الجزء الأول: المحرك الصاروخي (LMDB)
يتم تفويض المهام التي تتطلب **سرعة فورية** (Real-time) وتكراراً عالياً إلى LMDB:
*   **مصادقة الراديوس (Authentication):** عند اتصال مستخدم بالشبكة، يتم التحقق من بياناته بسرعة الضوء دون إشغال المعالج.
*   **حساب البيانات (Accounting):** الجلسات النشطة (Active Sessions) وتحديثات استهلاك الإنترنت (Data Usage) تُكتب أولاً في LMDB لسرعته.
*   *النتيجة:* استجابة لا تتعدى أجزاء من الملي ثانية (Sub-millisecond) لطلبات المايكروتك.

### 📊 الجزء الثاني: العقل الإداري (SQLite)
يتم تفويض المهام الإدارية التي تتطلب **تنظيماً وبحثاً** إلى SQLite:
*   **حسابات المشتركين وتفاصيلهم:** (الاسم، الهاتف، الباقة، الديون).
*   **المدراء والصلاحيات.**
*   **قوالب الواتساب والإعدادات العامة.**
*   **الفواتير والتقارير وسجل العمليات.**
*   *النتيجة:* لوحة تحكم سريعة الاستجابة للبحث والفلترة، وتقارير مالية دقيقة وسهلة الاستخراج.

## الخلاصة
محاولة وضع النظام بالكامل على LMDB ستجعل تطوير وإدارة واجهة المستخدم والتقارير كابوساً برمجياً دون فائدة حقيقية تذكر في الأداء. **البنية الهجينة الحالية هي المعيار الذهبي** للأنظمة المدمجة (Embedded Systems) وعالية الأداء مثل SASMAN.
